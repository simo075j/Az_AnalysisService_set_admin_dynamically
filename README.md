# Az_AnalysisService_set_admin_dynamically

The script can dynamically check and add both ad-users, ad-groups and serviceprincipals as server admins on a Azure Analysis service.

Simply assign the correct values to the variables in "<>". *be aware that the ADgroup variables needs to contain some value - use for example "na"*
